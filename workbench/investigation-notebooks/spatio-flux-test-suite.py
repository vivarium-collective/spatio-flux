#!/usr/bin/env python
"""Self-contained reproduction script — generated by vivarium-dashboard.

Run from anywhere with the workspace's virtualenv, e.g.:
    .venv/bin/python spatio-flux-test-suite.py

Figures are written to:  reports/notebooks/figures
Set RERUN = False (below) to render the committed runs.db without re-simulating.
"""
import os as _os
import sys as _sys

# YAML / study text is UTF-8; force UTF-8 mode so file reads don't depend on the
# shell locale (a bare-CLI run under an ASCII locale otherwise crashes on non-ASCII).
if _os.environ.get("PYTHONUTF8") != "1":
    _os.environ["PYTHONUTF8"] = "1"
    _os.execv(_sys.executable, [_sys.executable, *_sys.argv])


# # Spatio-Flux Test Suite — compositional multiscale scenarios
#
# _Investigation `spatio-flux-test-suite` — coder reproduction notebook._
#
# The spatio-flux reference application reproduced as one investigation: 19
# studies, each a composition scenario from the original test suite, arranged
# standalone processes → pairs → triples → reference demos.
#
# ---
#
# This notebook re-runs each study with the workspace's own process-bigraph protocol and renders its figures. The text states the **question and parameters** only — the figures produced by each run are the results. Set `RERUN = False` in the setup cell to render the committed `runs.db` without re-simulating.

"""Self-contained reproduction of this investigation.

Generated by vivarium-dashboard (notebook_export). Each study below is re-run
live with the workspace's own process-bigraph protocol and its figures are
rendered from the resulting runs.db.
"""
import os
import sys
from pathlib import Path

# Resolve the repository root robustly so this notebook runs from a fresh clone
# at ANY path with no setup (no env var, no path editing). Priority:
#   1. $VIVARIUM_REPO, if it points at a real directory;
#   2. walk up from the notebook's working directory for the repo markers
#      (a directory holding both 'workspace/' and 'pyproject.toml') — Jupyter
#      starts in the notebook's dir, so a committed notebook finds its own root;
#   3. the absolute path it was generated for (back-compat for old layouts);
#   4. the current working directory (last resort).
def _find_repo_root(_start):
    for _cand in (_start, *_start.parents):
        if (_cand / "workspace").is_dir() and (_cand / "pyproject.toml").is_file():
            return _cand
    return None

REPO = None
_env = os.environ.get("VIVARIUM_REPO")
if _env and Path(_env).is_dir():
    REPO = Path(_env)
if REPO is None:
    REPO = _find_repo_root(Path.cwd().resolve())
if REPO is None and Path('/home/runner/work/spatio-flux/spatio-flux').is_dir():
    REPO = Path('/home/runner/work/spatio-flux/spatio-flux')
if REPO is None:
    REPO = Path.cwd()
sys.path.insert(0, str(REPO))
# Composite specs use repo-root-relative paths (datasets, caches), and the
# workspace's runner/renderer assume cwd == repo root — so run from there.
os.chdir(REPO)

# Re-simulate from scratch? Set False to render the committed runs.db (fast).
RERUN = True

# --- standard process-bigraph protocol: register the workspace's Core ---
from spatio_flux.core import build_core
core = build_core()

# --- imported from the repo this notebook was generated for ---


import contextlib as _contextlib, io as _io
@_contextlib.contextmanager
def quiet():
    """Silence the simulator's verbose per-step stdout so the notebook
    output stays readable (the figures below are the results)."""
    with _contextlib.redirect_stdout(_io.StringIO()):
        yield

import html as _htmlmod
def show_viz(_h, height=560):
    """Display a visualization's HTML in an isolated iframe.

    The figures embed their own scripts (e.g. Plotly); JupyterLab does not
    execute <script> tags from display(HTML(...)), so an iframe srcdoc is
    used instead — the browser runs the scripts inside the frame."""
    display(HTML(
        '<iframe srcdoc="{}" style="width:100%;height:{}px;border:0">'
        '</iframe>'.format(_htmlmod.escape(_h, quote=True), height)
    ))

import json as _json
def describe_spec(spec):
    """Print a composite spec's structure (parameters, processes, wiring)
    then the full editable dict. The spec is plain data — assign to any
    field (e.g. spec['state'][proc]['config'][...]) before building."""
    print("composite:", spec.get("name"))
    if spec.get("description"):
        print("description:", str(spec["description"]).strip())
    _params = spec.get("parameters") or {}
    if _params:
        print("\nparameters (filled into ${name} placeholders):")
        for _p, _pdef in _params.items():
            print(f"  {_p}: default={_pdef.get('default')!r}  type={_pdef.get('type')}")
    print("\nprocesses (node -> address):")
    for _node, _body in (spec.get("state") or {}).items():
        if not (isinstance(_body, dict) and _body.get("_type") == "process"):
            continue
        print(f"  {_node}  ->  {_body.get('address')}   interval={_body.get('interval')!r}")
        for _port in ("inputs", "outputs"):
            if _body.get(_port):
                print(f"      {_port} ports: {_body[_port]}")
    print("\nfull editable spec dict:")
    print(_json.dumps(spec, indent=2, default=str))

import base64 as _b64, pathlib as _pl
def _render_one(address, config, runs_db, study_yaml):
    """Generic figure renderer (no workspace render_study_viz.py):
    resolve an ``image:<relpath>`` visualization to displayable HTML,
    relative to the study directory."""
    addr = str(address or '')
    for _scheme in ('image:', 'file:', 'gif:', 'png:', 'svg:', 'jpg:', 'jpeg:'):
        if addr.startswith(_scheme):
            addr = addr[len(_scheme):]; break
    _p = _pl.Path(addr)
    if not _p.is_absolute():
        _p = _pl.Path(study_yaml).resolve().parent / _p
    if not _p.is_file():
        return f'<p style="color:#b91c1c">figure not found: {address}</p>'
    _suffix = _p.suffix.lower()
    if _suffix == '.svg':
        return _p.read_text(encoding='utf-8', errors='replace')
    if _suffix in ('.png', '.jpg', '.jpeg', '.gif', '.webp'):
        _mime = 'jpeg' if _suffix in ('.jpg', '.jpeg') else _suffix[1:]
        _data = _b64.b64encode(_p.read_bytes()).decode('ascii')
        return f'<img src="data:image/{_mime};base64,{_data}" style="max-width:100%"/>'
    if _suffix in ('.html', '.htm'):
        return _p.read_text(encoding='utf-8', errors='replace')
    return f'<p style="color:#6b7280">unsupported figure type: {address}</p>'

# ## Study: `monod_kinetics`
#
# **Purpose.** Runs the spatio_flux.composites.metabolism.monod_kinetics composite (standalone process).

# ### Parameters
#
# | simulation | composite | steps | params |
# | --- | --- | --- | --- |
# | `baseline` | `spatio_flux.composites.metabolism.monod_kinetics` | 0 | model_id=overflow_metabolism |

# ### Specification (process-bigraph) — load, inspect, edit
#
# Each composite is a process-bigraph *document*: named processes (`_type: process`) bound to an `address`, wired by `inputs`/`outputs` ports over shared stores. For every composite below the first cell loads the spec into a plain **editable Python dict** and prints its structure; the second cell is a **control panel** listing every configuration value and per-process `interval` so you can tweak any of them. Your edits are read when the composite is built and run, in the **Run** section.

# **Composite `spatio_flux.composites.metabolism.monod_kinetics`** — `spec_spatio_flux_composites_metabolism_monod_kinetics` (a plain, editable dict)

# _composite spec file for `spatio_flux.composites.metabolism.monod_kinetics` not found under `spatio_flux/composites/` — skipped._

# ### Run
#
# _Set the runtime (`STEPS`) and step size (`INTERVAL`), then run. Each simulation builds the (edited) spec above and writes `runs.db`; the figures below read it. Set `RERUN = False` to skip re-simulating._

# === Study: monod_kinetics ===
STUDY = 'monod_kinetics'
STUDY_DIR = REPO / 'studies' / STUDY
STUDY_YAML = str(STUDY_DIR / "study.yaml")
RUNS_DB = str(STUDY_DIR / "runs.db")

print("No recorded runs for this study; nothing to reproduce.")

# ### Visualizations
#
# _Results are shown by the figures below, produced by the run above._

# **monod_kinetics.svg**

def _save_viz(study, slug, html):
    d = REPO / 'reports/notebooks/figures' / study
    d.mkdir(parents=True, exist_ok=True)
    out = d / (slug + '.html')
    out.write_text(html, encoding='utf-8')
    print('  wrote', out)


# monod_kinetics.svg
_save_viz('monod_kinetics', 'monod_kinetics.svg', _render_one('image:visualizations/monod_kinetics.svg', {}, RUNS_DB, STUDY_YAML))

# **monod_kinetics.png**

# monod_kinetics.png
_save_viz('monod_kinetics', 'monod_kinetics.png', _render_one('image:charts/monod_kinetics.png', {}, RUNS_DB, STUDY_YAML))

# ### Acceptance criteria
#
# _Pre-registered checks (criteria/thresholds only — run the cells above to evaluate them)._
#
# | test | measures | passes if |
# | --- | --- | --- |
# | MONOD_KINETICS-REPRODUCES-REPORT | kind=artifacts_present expected=['monod_kinetics.png', 'monod_kinetics_viz.png', 'monod_kinetics_schema.json', 'monod_kinetics_state.json'] | op all_exist_and_match tolerance 0.02 |

# ## Study: `ecoli_core_dfba`
#
# **Purpose.** Runs the spatio_flux.composites.metabolism.ecoli_core_dfba composite (standalone process).

# ### Parameters
#
# | simulation | composite | steps | params |
# | --- | --- | --- | --- |
# | `baseline` | `spatio_flux.composites.metabolism.ecoli_core_dfba` | 0 | model_id=ecoli core, glucose=10.0, acetate=0.0 |

# ### Specification (process-bigraph) — load, inspect, edit
#
# Each composite is a process-bigraph *document*: named processes (`_type: process`) bound to an `address`, wired by `inputs`/`outputs` ports over shared stores. For every composite below the first cell loads the spec into a plain **editable Python dict** and prints its structure; the second cell is a **control panel** listing every configuration value and per-process `interval` so you can tweak any of them. Your edits are read when the composite is built and run, in the **Run** section.

# **Composite `spatio_flux.composites.metabolism.ecoli_core_dfba`** — `spec_spatio_flux_composites_metabolism_ecoli_core_dfba` (a plain, editable dict)

# _composite spec file for `spatio_flux.composites.metabolism.ecoli_core_dfba` not found under `spatio_flux/composites/` — skipped._

# ### Run
#
# _Set the runtime (`STEPS`) and step size (`INTERVAL`), then run. Each simulation builds the (edited) spec above and writes `runs.db`; the figures below read it. Set `RERUN = False` to skip re-simulating._

# === Study: ecoli_core_dfba ===
STUDY = 'ecoli_core_dfba'
STUDY_DIR = REPO / 'studies' / STUDY
STUDY_YAML = str(STUDY_DIR / "study.yaml")
RUNS_DB = str(STUDY_DIR / "runs.db")

print("No recorded runs for this study; nothing to reproduce.")

# ### Visualizations
#
# _Results are shown by the figures below, produced by the run above._

# **ecoli_core_dfba.svg**

# ecoli_core_dfba.svg
_save_viz('ecoli_core_dfba', 'ecoli_core_dfba.svg', _render_one('image:visualizations/ecoli_core_dfba.svg', {}, RUNS_DB, STUDY_YAML))

# **ecoli_core_dfba.png**

# ecoli_core_dfba.png
_save_viz('ecoli_core_dfba', 'ecoli_core_dfba.png', _render_one('image:charts/ecoli_core_dfba.png', {}, RUNS_DB, STUDY_YAML))

# ### Acceptance criteria
#
# _Pre-registered checks (criteria/thresholds only — run the cells above to evaluate them)._
#
# | test | measures | passes if |
# | --- | --- | --- |
# | ECOLI_CORE_DFBA-REPRODUCES-REPORT | kind=artifacts_present expected=['ecoli_core_dfba.png', 'ecoli_core_dfba_viz.png', 'ecoli_core_dfba_schema.json', 'ecoli_core_dfba_state.json'] | op all_exist_and_match tolerance 0.02 |

# ## Study: `ecoli_dfba`
#
# **Purpose.** Runs the spatio_flux.composites.metabolism.ecoli_dfba composite (standalone process).

# ### Parameters
#
# | simulation | composite | steps | params |
# | --- | --- | --- | --- |
# | `baseline` | `spatio_flux.composites.metabolism.ecoli_dfba` | 0 | model_id=ecoli, glucose=10.0, formate=5.0 |

# ### Specification (process-bigraph) — load, inspect, edit
#
# Each composite is a process-bigraph *document*: named processes (`_type: process`) bound to an `address`, wired by `inputs`/`outputs` ports over shared stores. For every composite below the first cell loads the spec into a plain **editable Python dict** and prints its structure; the second cell is a **control panel** listing every configuration value and per-process `interval` so you can tweak any of them. Your edits are read when the composite is built and run, in the **Run** section.

# **Composite `spatio_flux.composites.metabolism.ecoli_dfba`** — `spec_spatio_flux_composites_metabolism_ecoli_dfba` (a plain, editable dict)

# _composite spec file for `spatio_flux.composites.metabolism.ecoli_dfba` not found under `spatio_flux/composites/` — skipped._

# ### Run
#
# _Set the runtime (`STEPS`) and step size (`INTERVAL`), then run. Each simulation builds the (edited) spec above and writes `runs.db`; the figures below read it. Set `RERUN = False` to skip re-simulating._

# === Study: ecoli_dfba ===
STUDY = 'ecoli_dfba'
STUDY_DIR = REPO / 'studies' / STUDY
STUDY_YAML = str(STUDY_DIR / "study.yaml")
RUNS_DB = str(STUDY_DIR / "runs.db")

print("No recorded runs for this study; nothing to reproduce.")

# ### Visualizations
#
# _Results are shown by the figures below, produced by the run above._

# **ecoli_dfba.svg**

# ecoli_dfba.svg
_save_viz('ecoli_dfba', 'ecoli_dfba.svg', _render_one('image:visualizations/ecoli_dfba.svg', {}, RUNS_DB, STUDY_YAML))

# **ecoli_dfba.png**

# ecoli_dfba.png
_save_viz('ecoli_dfba', 'ecoli_dfba.png', _render_one('image:charts/ecoli_dfba.png', {}, RUNS_DB, STUDY_YAML))

# ### Acceptance criteria
#
# _Pre-registered checks (criteria/thresholds only — run the cells above to evaluate them)._
#
# | test | measures | passes if |
# | --- | --- | --- |
# | ECOLI_DFBA-REPRODUCES-REPORT | kind=artifacts_present expected=['ecoli_dfba.png', 'ecoli_dfba_viz.png', 'ecoli_dfba_schema.json', 'ecoli_dfba_state.json'] | op all_exist_and_match tolerance 0.02 |

# ## Study: `yeast_dfba`
#
# **Purpose.** Runs the spatio_flux.composites.metabolism.yeast_dfba composite (standalone process).

# ### Parameters
#
# | simulation | composite | steps | params |
# | --- | --- | --- | --- |
# | `baseline` | `spatio_flux.composites.metabolism.yeast_dfba` | 0 | model_id=yeast, glucose=5.0 |

# ### Specification (process-bigraph) — load, inspect, edit
#
# Each composite is a process-bigraph *document*: named processes (`_type: process`) bound to an `address`, wired by `inputs`/`outputs` ports over shared stores. For every composite below the first cell loads the spec into a plain **editable Python dict** and prints its structure; the second cell is a **control panel** listing every configuration value and per-process `interval` so you can tweak any of them. Your edits are read when the composite is built and run, in the **Run** section.

# **Composite `spatio_flux.composites.metabolism.yeast_dfba`** — `spec_spatio_flux_composites_metabolism_yeast_dfba` (a plain, editable dict)

# _composite spec file for `spatio_flux.composites.metabolism.yeast_dfba` not found under `spatio_flux/composites/` — skipped._

# ### Run
#
# _Set the runtime (`STEPS`) and step size (`INTERVAL`), then run. Each simulation builds the (edited) spec above and writes `runs.db`; the figures below read it. Set `RERUN = False` to skip re-simulating._

# === Study: yeast_dfba ===
STUDY = 'yeast_dfba'
STUDY_DIR = REPO / 'studies' / STUDY
STUDY_YAML = str(STUDY_DIR / "study.yaml")
RUNS_DB = str(STUDY_DIR / "runs.db")

print("No recorded runs for this study; nothing to reproduce.")

# ### Visualizations
#
# _Results are shown by the figures below, produced by the run above._

# **yeast_dfba.svg**

# yeast_dfba.svg
_save_viz('yeast_dfba', 'yeast_dfba.svg', _render_one('image:visualizations/yeast_dfba.svg', {}, RUNS_DB, STUDY_YAML))

# **yeast_dfba.png**

# yeast_dfba.png
_save_viz('yeast_dfba', 'yeast_dfba.png', _render_one('image:charts/yeast_dfba.png', {}, RUNS_DB, STUDY_YAML))

# ### Acceptance criteria
#
# _Pre-registered checks (criteria/thresholds only — run the cells above to evaluate them)._
#
# | test | measures | passes if |
# | --- | --- | --- |
# | YEAST_DFBA-REPRODUCES-REPORT | kind=artifacts_present expected=['yeast_dfba.png', 'yeast_dfba_viz.png', 'yeast_dfba_schema.json', 'yeast_dfba_state.json'] | op all_exist_and_match tolerance 0.02 |

# ## Study: `diffusion_process`
#
# **Purpose.** Runs the spatio_flux.composites.spatial.diffusion_process composite (standalone process).

# ### Parameters
#
# | simulation | composite | steps | params |
# | --- | --- | --- | --- |
# | `baseline` | `spatio_flux.composites.spatial.diffusion_process` | 0 | — |

# ### Specification (process-bigraph) — load, inspect, edit
#
# Each composite is a process-bigraph *document*: named processes (`_type: process`) bound to an `address`, wired by `inputs`/`outputs` ports over shared stores. For every composite below the first cell loads the spec into a plain **editable Python dict** and prints its structure; the second cell is a **control panel** listing every configuration value and per-process `interval` so you can tweak any of them. Your edits are read when the composite is built and run, in the **Run** section.

# **Composite `spatio_flux.composites.spatial.diffusion_process`** — `spec_spatio_flux_composites_spatial_diffusion_process` (a plain, editable dict)

# _composite spec file for `spatio_flux.composites.spatial.diffusion_process` not found under `spatio_flux/composites/` — skipped._

# ### Run
#
# _Set the runtime (`STEPS`) and step size (`INTERVAL`), then run. Each simulation builds the (edited) spec above and writes `runs.db`; the figures below read it. Set `RERUN = False` to skip re-simulating._

# === Study: diffusion_process ===
STUDY = 'diffusion_process'
STUDY_DIR = REPO / 'studies' / STUDY
STUDY_YAML = str(STUDY_DIR / "study.yaml")
RUNS_DB = str(STUDY_DIR / "runs.db")

print("No recorded runs for this study; nothing to reproduce.")

# ### Visualizations
#
# _Results are shown by the figures below, produced by the run above._

# **diffusion_process.svg**

# diffusion_process.svg
_save_viz('diffusion_process', 'diffusion_process.svg', _render_one('image:visualizations/diffusion_process.svg', {}, RUNS_DB, STUDY_YAML))

# ### Acceptance criteria
#
# _Pre-registered checks (criteria/thresholds only — run the cells above to evaluate them)._
#
# | test | measures | passes if |
# | --- | --- | --- |
# | DIFFUSION_PROCESS-REPRODUCES-REPORT | kind=artifacts_present expected=['diffusion_process_video.gif', 'diffusion_process_viz.png', 'diffusion_process_schema.json', 'diffusion_process_state.json'] | op all_exist_and_match tolerance 0.02 |

# ## Study: `brownian_particles`
#
# **Purpose.** Runs the spatio_flux.composites.particles.brownian_particles composite (standalone process).

# ### Parameters
#
# | simulation | composite | steps | params |
# | --- | --- | --- | --- |
# | `baseline` | `spatio_flux.composites.particles.brownian_particles` | 0 | — |

# ### Specification (process-bigraph) — load, inspect, edit
#
# Each composite is a process-bigraph *document*: named processes (`_type: process`) bound to an `address`, wired by `inputs`/`outputs` ports over shared stores. For every composite below the first cell loads the spec into a plain **editable Python dict** and prints its structure; the second cell is a **control panel** listing every configuration value and per-process `interval` so you can tweak any of them. Your edits are read when the composite is built and run, in the **Run** section.

# **Composite `spatio_flux.composites.particles.brownian_particles`** — `spec_spatio_flux_composites_particles_brownian_particles` (a plain, editable dict)

# _composite spec file for `spatio_flux.composites.particles.brownian_particles` not found under `spatio_flux/composites/` — skipped._

# ### Run
#
# _Set the runtime (`STEPS`) and step size (`INTERVAL`), then run. Each simulation builds the (edited) spec above and writes `runs.db`; the figures below read it. Set `RERUN = False` to skip re-simulating._

# === Study: brownian_particles ===
STUDY = 'brownian_particles'
STUDY_DIR = REPO / 'studies' / STUDY
STUDY_YAML = str(STUDY_DIR / "study.yaml")
RUNS_DB = str(STUDY_DIR / "runs.db")

print("No recorded runs for this study; nothing to reproduce.")

# ### Visualizations
#
# _Results are shown by the figures below, produced by the run above._

# **brownian_particles.svg**

# brownian_particles.svg
_save_viz('brownian_particles', 'brownian_particles.svg', _render_one('image:visualizations/brownian_particles.svg', {}, RUNS_DB, STUDY_YAML))

# **brownian_particles_snapshots.png**

# brownian_particles_snapshots.png
_save_viz('brownian_particles', 'brownian_particles_snapshots.png', _render_one('image:charts/brownian_particles_snapshots.png', {}, RUNS_DB, STUDY_YAML))

# **brownian_particles_particles_traces.png**

# brownian_particles_particles_traces.png
_save_viz('brownian_particles', 'brownian_particles_particles_traces.png', _render_one('image:charts/brownian_particles_particles_traces.png', {}, RUNS_DB, STUDY_YAML))

# **brownian_particles_mass.png**

# brownian_particles_mass.png
_save_viz('brownian_particles', 'brownian_particles_mass.png', _render_one('image:charts/brownian_particles_mass.png', {}, RUNS_DB, STUDY_YAML))

# ### Acceptance criteria
#
# _Pre-registered checks (criteria/thresholds only — run the cells above to evaluate them)._
#
# | test | measures | passes if |
# | --- | --- | --- |
# | BROWNIAN_PARTICLES-REPRODUCES-REPORT | kind=artifacts_present expected=['brownian_particles_video.gif', 'brownian_particles_mass.png', 'brownian_particles_snapshots.png', 'brownian_particles_particles_traces.png', 'brownian_particles_viz.png', 'brownian_particles_schema.json', 'brownian_particles_state.json'] | op all_exist_and_match tolerance 0.02 |

# ## Study: `newtonian_particles`
#
# **Purpose.** Runs the spatio_flux.composites.particles.newtonian_particles composite (standalone process).

# ### Parameters
#
# | simulation | composite | steps | params |
# | --- | --- | --- | --- |
# | `baseline` | `spatio_flux.composites.particles.newtonian_particles` | 0 | — |

# ### Specification (process-bigraph) — load, inspect, edit
#
# Each composite is a process-bigraph *document*: named processes (`_type: process`) bound to an `address`, wired by `inputs`/`outputs` ports over shared stores. For every composite below the first cell loads the spec into a plain **editable Python dict** and prints its structure; the second cell is a **control panel** listing every configuration value and per-process `interval` so you can tweak any of them. Your edits are read when the composite is built and run, in the **Run** section.

# **Composite `spatio_flux.composites.particles.newtonian_particles`** — `spec_spatio_flux_composites_particles_newtonian_particles` (a plain, editable dict)

# _composite spec file for `spatio_flux.composites.particles.newtonian_particles` not found under `spatio_flux/composites/` — skipped._

# ### Run
#
# _Set the runtime (`STEPS`) and step size (`INTERVAL`), then run. Each simulation builds the (edited) spec above and writes `runs.db`; the figures below read it. Set `RERUN = False` to skip re-simulating._

# === Study: newtonian_particles ===
STUDY = 'newtonian_particles'
STUDY_DIR = REPO / 'studies' / STUDY
STUDY_YAML = str(STUDY_DIR / "study.yaml")
RUNS_DB = str(STUDY_DIR / "runs.db")

print("No recorded runs for this study; nothing to reproduce.")

# ### Visualizations
#
# _Results are shown by the figures below, produced by the run above._

# **newtonian_particles.svg**

# newtonian_particles.svg
_save_viz('newtonian_particles', 'newtonian_particles.svg', _render_one('image:visualizations/newtonian_particles.svg', {}, RUNS_DB, STUDY_YAML))

# **newtonian_particles_particles_traces.png**

# newtonian_particles_particles_traces.png
_save_viz('newtonian_particles', 'newtonian_particles_particles_traces.png', _render_one('image:charts/newtonian_particles_particles_traces.png', {}, RUNS_DB, STUDY_YAML))

# **newtonian_particles_mass.png**

# newtonian_particles_mass.png
_save_viz('newtonian_particles', 'newtonian_particles_mass.png', _render_one('image:charts/newtonian_particles_mass.png', {}, RUNS_DB, STUDY_YAML))

# ### Acceptance criteria
#
# _Pre-registered checks (criteria/thresholds only — run the cells above to evaluate them)._
#
# | test | measures | passes if |
# | --- | --- | --- |
# | NEWTONIAN_PARTICLES-REPRODUCES-REPORT | kind=artifacts_present expected=['newtonian_particles_mass.png', 'newtonian_particles_video.gif', 'newtonian_particles_particles_traces.png', 'newtonian_particles_viz.png', 'newtonian_particles_schema.json', 'newtonian_particles_state.json'] | op all_exist_and_match tolerance 0.02 |

# ## Study: `community_dfba`
#
# **Purpose.** Runs the spatio_flux.composites.metabolism.community_dfba composite (pair composition).

# ### Parameters
#
# | simulation | composite | steps | params |
# | --- | --- | --- | --- |
# | `baseline` | `spatio_flux.composites.metabolism.community_dfba` | 0 | — |

# ### Specification (process-bigraph) — load, inspect, edit
#
# Each composite is a process-bigraph *document*: named processes (`_type: process`) bound to an `address`, wired by `inputs`/`outputs` ports over shared stores. For every composite below the first cell loads the spec into a plain **editable Python dict** and prints its structure; the second cell is a **control panel** listing every configuration value and per-process `interval` so you can tweak any of them. Your edits are read when the composite is built and run, in the **Run** section.

# **Composite `spatio_flux.composites.metabolism.community_dfba`** — `spec_spatio_flux_composites_metabolism_community_dfba` (a plain, editable dict)

# _composite spec file for `spatio_flux.composites.metabolism.community_dfba` not found under `spatio_flux/composites/` — skipped._

# ### Run
#
# _Set the runtime (`STEPS`) and step size (`INTERVAL`), then run. Each simulation builds the (edited) spec above and writes `runs.db`; the figures below read it. Set `RERUN = False` to skip re-simulating._

# === Study: community_dfba ===
STUDY = 'community_dfba'
STUDY_DIR = REPO / 'studies' / STUDY
STUDY_YAML = str(STUDY_DIR / "study.yaml")
RUNS_DB = str(STUDY_DIR / "runs.db")

print("No recorded runs for this study; nothing to reproduce.")

# ### Visualizations
#
# _Results are shown by the figures below, produced by the run above._

# **community_dfba.svg**

# community_dfba.svg
_save_viz('community_dfba', 'community_dfba.svg', _render_one('image:visualizations/community_dfba.svg', {}, RUNS_DB, STUDY_YAML))

# **community_dfba.png**

# community_dfba.png
_save_viz('community_dfba', 'community_dfba.png', _render_one('image:charts/community_dfba.png', {}, RUNS_DB, STUDY_YAML))

# ### Acceptance criteria
#
# _Pre-registered checks (criteria/thresholds only — run the cells above to evaluate them)._
#
# | test | measures | passes if |
# | --- | --- | --- |
# | COMMUNITY_DFBA-REPRODUCES-REPORT | kind=artifacts_present expected=['community_dfba.png', 'community_dfba_viz.png', 'community_dfba_schema.json', 'community_dfba_state.json'] | op all_exist_and_match tolerance 0.02 |

# ## Study: `dfba_kinetics_community`
#
# **Purpose.** Runs the spatio_flux.composites.metabolism.dfba_kinetics_community composite (pair composition).

# ### Parameters
#
# | simulation | composite | steps | params |
# | --- | --- | --- | --- |
# | `baseline` | `spatio_flux.composites.metabolism.dfba_kinetics_community` | 0 | — |

# ### Specification (process-bigraph) — load, inspect, edit
#
# Each composite is a process-bigraph *document*: named processes (`_type: process`) bound to an `address`, wired by `inputs`/`outputs` ports over shared stores. For every composite below the first cell loads the spec into a plain **editable Python dict** and prints its structure; the second cell is a **control panel** listing every configuration value and per-process `interval` so you can tweak any of them. Your edits are read when the composite is built and run, in the **Run** section.

# **Composite `spatio_flux.composites.metabolism.dfba_kinetics_community`** — `spec_spatio_flux_composites_metabolism_dfba_kinetics_community` (a plain, editable dict)

# _composite spec file for `spatio_flux.composites.metabolism.dfba_kinetics_community` not found under `spatio_flux/composites/` — skipped._

# ### Run
#
# _Set the runtime (`STEPS`) and step size (`INTERVAL`), then run. Each simulation builds the (edited) spec above and writes `runs.db`; the figures below read it. Set `RERUN = False` to skip re-simulating._

# === Study: dfba_kinetics_community ===
STUDY = 'dfba_kinetics_community'
STUDY_DIR = REPO / 'studies' / STUDY
STUDY_YAML = str(STUDY_DIR / "study.yaml")
RUNS_DB = str(STUDY_DIR / "runs.db")

print("No recorded runs for this study; nothing to reproduce.")

# ### Visualizations
#
# _Results are shown by the figures below, produced by the run above._

# **dfba_kinetics_community.svg**

# dfba_kinetics_community.svg
_save_viz('dfba_kinetics_community', 'dfba_kinetics_community.svg', _render_one('image:visualizations/dfba_kinetics_community.svg', {}, RUNS_DB, STUDY_YAML))

# **dfba_kinetics_community.png**

# dfba_kinetics_community.png
_save_viz('dfba_kinetics_community', 'dfba_kinetics_community.png', _render_one('image:charts/dfba_kinetics_community.png', {}, RUNS_DB, STUDY_YAML))

# ### Acceptance criteria
#
# _Pre-registered checks (criteria/thresholds only — run the cells above to evaluate them)._
#
# | test | measures | passes if |
# | --- | --- | --- |
# | DFBA_KINETICS_COMMUNITY-REPRODUCES-REPORT | kind=artifacts_present expected=['dfba_kinetics_community.png', 'dfba_kinetics_community_viz.png', 'dfba_kinetics_community_schema.json', 'dfba_kinetics_community_state.json'] | op all_exist_and_match tolerance 0.02 |

# ## Study: `spatial_many_dfba`
#
# **Purpose.** Runs the spatio_flux.composites.spatial.spatial_many_dfba composite (pair composition).

# ### Parameters
#
# | simulation | composite | steps | params |
# | --- | --- | --- | --- |
# | `baseline` | `spatio_flux.composites.spatial.spatial_many_dfba` | 0 | model_id=ecoli core |

# ### Specification (process-bigraph) — load, inspect, edit
#
# Each composite is a process-bigraph *document*: named processes (`_type: process`) bound to an `address`, wired by `inputs`/`outputs` ports over shared stores. For every composite below the first cell loads the spec into a plain **editable Python dict** and prints its structure; the second cell is a **control panel** listing every configuration value and per-process `interval` so you can tweak any of them. Your edits are read when the composite is built and run, in the **Run** section.

# **Composite `spatio_flux.composites.spatial.spatial_many_dfba`** — `spec_spatio_flux_composites_spatial_spatial_many_dfba` (a plain, editable dict)

# _composite spec file for `spatio_flux.composites.spatial.spatial_many_dfba` not found under `spatio_flux/composites/` — skipped._

# ### Run
#
# _Set the runtime (`STEPS`) and step size (`INTERVAL`), then run. Each simulation builds the (edited) spec above and writes `runs.db`; the figures below read it. Set `RERUN = False` to skip re-simulating._

# === Study: spatial_many_dfba ===
STUDY = 'spatial_many_dfba'
STUDY_DIR = REPO / 'studies' / STUDY
STUDY_YAML = str(STUDY_DIR / "study.yaml")
RUNS_DB = str(STUDY_DIR / "runs.db")

print("No recorded runs for this study; nothing to reproduce.")

# ### Visualizations
#
# _Results are shown by the figures below, produced by the run above._

# **spatial_many_dfba.svg**

# spatial_many_dfba.svg
_save_viz('spatial_many_dfba', 'spatial_many_dfba.svg', _render_one('image:visualizations/spatial_many_dfba.svg', {}, RUNS_DB, STUDY_YAML))

# **spatial_many_dfba_timeseries.png**

# spatial_many_dfba_timeseries.png
_save_viz('spatial_many_dfba', 'spatial_many_dfba_timeseries.png', _render_one('image:charts/spatial_many_dfba_timeseries.png', {}, RUNS_DB, STUDY_YAML))

# ### Acceptance criteria
#
# _Pre-registered checks (criteria/thresholds only — run the cells above to evaluate them)._
#
# | test | measures | passes if |
# | --- | --- | --- |
# | SPATIAL_MANY_DFBA-REPRODUCES-REPORT | kind=artifacts_present expected=['spatial_many_dfba_timeseries.png', 'spatial_many_dfba_video.gif', 'spatial_many_dfba_viz.png', 'spatial_many_dfba_schema.json', 'spatial_many_dfba_state.json'] | op all_exist_and_match tolerance 0.02 |

# ## Study: `spatial_dfba_process`
#
# **Purpose.** Runs the spatio_flux.composites.spatial.spatial_dfba_process composite (pair composition).

# ### Parameters
#
# | simulation | composite | steps | params |
# | --- | --- | --- | --- |
# | `baseline` | `spatio_flux.composites.spatial.spatial_dfba_process` | 0 | — |

# ### Specification (process-bigraph) — load, inspect, edit
#
# Each composite is a process-bigraph *document*: named processes (`_type: process`) bound to an `address`, wired by `inputs`/`outputs` ports over shared stores. For every composite below the first cell loads the spec into a plain **editable Python dict** and prints its structure; the second cell is a **control panel** listing every configuration value and per-process `interval` so you can tweak any of them. Your edits are read when the composite is built and run, in the **Run** section.

# **Composite `spatio_flux.composites.spatial.spatial_dfba_process`** — `spec_spatio_flux_composites_spatial_spatial_dfba_process` (a plain, editable dict)

# _composite spec file for `spatio_flux.composites.spatial.spatial_dfba_process` not found under `spatio_flux/composites/` — skipped._

# ### Run
#
# _Set the runtime (`STEPS`) and step size (`INTERVAL`), then run. Each simulation builds the (edited) spec above and writes `runs.db`; the figures below read it. Set `RERUN = False` to skip re-simulating._

# === Study: spatial_dfba_process ===
STUDY = 'spatial_dfba_process'
STUDY_DIR = REPO / 'studies' / STUDY
STUDY_YAML = str(STUDY_DIR / "study.yaml")
RUNS_DB = str(STUDY_DIR / "runs.db")

print("No recorded runs for this study; nothing to reproduce.")

# ### Visualizations
#
# _Results are shown by the figures below, produced by the run above._

# **spatial_dfba_process.svg**

# spatial_dfba_process.svg
_save_viz('spatial_dfba_process', 'spatial_dfba_process.svg', _render_one('image:visualizations/spatial_dfba_process.svg', {}, RUNS_DB, STUDY_YAML))

# **spatial_dfba_process_timeseries.png**

# spatial_dfba_process_timeseries.png
_save_viz('spatial_dfba_process', 'spatial_dfba_process_timeseries.png', _render_one('image:charts/spatial_dfba_process_timeseries.png', {}, RUNS_DB, STUDY_YAML))

# **spatial_dfba_process_model_grid.png**

# spatial_dfba_process_model_grid.png
_save_viz('spatial_dfba_process', 'spatial_dfba_process_model_grid.png', _render_one('image:charts/spatial_dfba_process_model_grid.png', {}, RUNS_DB, STUDY_YAML))

# ### Acceptance criteria
#
# _Pre-registered checks (criteria/thresholds only — run the cells above to evaluate them)._
#
# | test | measures | passes if |
# | --- | --- | --- |
# | SPATIAL_DFBA_PROCESS-REPRODUCES-REPORT | kind=artifacts_present expected=['spatial_dfba_process_timeseries.png', 'spatial_dfba_process_model_grid.png', 'spatial_dfba_process_video.gif', 'spatial_dfba_process_viz.png', 'spatial_dfba_process_schema.json', 'spatial_dfba_process_state.json'] | op all_exist_and_match tolerance 0.02 |

# ## Study: `comets_diffusion`
#
# **Purpose.** Runs the spatio_flux.composites.comets.comets_diffusion composite (pair composition).

# ### Parameters
#
# | simulation | composite | steps | params |
# | --- | --- | --- | --- |
# | `baseline` | `spatio_flux.composites.comets.comets_diffusion` | 0 | — |

# ### Specification (process-bigraph) — load, inspect, edit
#
# Each composite is a process-bigraph *document*: named processes (`_type: process`) bound to an `address`, wired by `inputs`/`outputs` ports over shared stores. For every composite below the first cell loads the spec into a plain **editable Python dict** and prints its structure; the second cell is a **control panel** listing every configuration value and per-process `interval` so you can tweak any of them. Your edits are read when the composite is built and run, in the **Run** section.

# **Composite `spatio_flux.composites.comets.comets_diffusion`** — `spec_spatio_flux_composites_comets_comets_diffusion` (a plain, editable dict)

# _composite spec file for `spatio_flux.composites.comets.comets_diffusion` not found under `spatio_flux/composites/` — skipped._

# ### Run
#
# _Set the runtime (`STEPS`) and step size (`INTERVAL`), then run. Each simulation builds the (edited) spec above and writes `runs.db`; the figures below read it. Set `RERUN = False` to skip re-simulating._

# === Study: comets_diffusion ===
STUDY = 'comets_diffusion'
STUDY_DIR = REPO / 'studies' / STUDY
STUDY_YAML = str(STUDY_DIR / "study.yaml")
RUNS_DB = str(STUDY_DIR / "runs.db")

print("No recorded runs for this study; nothing to reproduce.")

# ### Visualizations
#
# _Results are shown by the figures below, produced by the run above._

# **comets_diffusion.svg**

# comets_diffusion.svg
_save_viz('comets_diffusion', 'comets_diffusion.svg', _render_one('image:visualizations/comets_diffusion.svg', {}, RUNS_DB, STUDY_YAML))

# **comets_diffusion_timeseries.png**

# comets_diffusion_timeseries.png
_save_viz('comets_diffusion', 'comets_diffusion_timeseries.png', _render_one('image:charts/comets_diffusion_timeseries.png', {}, RUNS_DB, STUDY_YAML))

# **comets_diffusion_snapshots.png**

# comets_diffusion_snapshots.png
_save_viz('comets_diffusion', 'comets_diffusion_snapshots.png', _render_one('image:charts/comets_diffusion_snapshots.png', {}, RUNS_DB, STUDY_YAML))

# ### Acceptance criteria
#
# _Pre-registered checks (criteria/thresholds only — run the cells above to evaluate them)._
#
# | test | measures | passes if |
# | --- | --- | --- |
# | COMETS_DIFFUSION-REPRODUCES-REPORT | kind=artifacts_present expected=['comets_diffusion_timeseries.png', 'comets_diffusion_snapshots.png', 'comets_diffusion_video.gif', 'comets_diffusion_viz.png', 'comets_diffusion_schema.json', 'comets_diffusion_state.json'] | op all_exist_and_match tolerance 0.02 |

# ## Study: `br_particles_kinetics`
#
# **Purpose.** Runs the spatio_flux.composites.particles.br_particles_kinetics composite (pair composition).

# ### Parameters
#
# | simulation | composite | steps | params |
# | --- | --- | --- | --- |
# | `baseline` | `spatio_flux.composites.particles.br_particles_kinetics` | 0 | — |

# ### Specification (process-bigraph) — load, inspect, edit
#
# Each composite is a process-bigraph *document*: named processes (`_type: process`) bound to an `address`, wired by `inputs`/`outputs` ports over shared stores. For every composite below the first cell loads the spec into a plain **editable Python dict** and prints its structure; the second cell is a **control panel** listing every configuration value and per-process `interval` so you can tweak any of them. Your edits are read when the composite is built and run, in the **Run** section.

# **Composite `spatio_flux.composites.particles.br_particles_kinetics`** — `spec_spatio_flux_composites_particles_br_particles_kinetics` (a plain, editable dict)

# _composite spec file for `spatio_flux.composites.particles.br_particles_kinetics` not found under `spatio_flux/composites/` — skipped._

# ### Run
#
# _Set the runtime (`STEPS`) and step size (`INTERVAL`), then run. Each simulation builds the (edited) spec above and writes `runs.db`; the figures below read it. Set `RERUN = False` to skip re-simulating._

# === Study: br_particles_kinetics ===
STUDY = 'br_particles_kinetics'
STUDY_DIR = REPO / 'studies' / STUDY
STUDY_YAML = str(STUDY_DIR / "study.yaml")
RUNS_DB = str(STUDY_DIR / "runs.db")

print("No recorded runs for this study; nothing to reproduce.")

# ### Visualizations
#
# _Results are shown by the figures below, produced by the run above._

# **br_particles_kinetics.svg**

# br_particles_kinetics.svg
_save_viz('br_particles_kinetics', 'br_particles_kinetics.svg', _render_one('image:visualizations/br_particles_kinetics.svg', {}, RUNS_DB, STUDY_YAML))

# **br_particles_kinetics_snapshots.png**

# br_particles_kinetics_snapshots.png
_save_viz('br_particles_kinetics', 'br_particles_kinetics_snapshots.png', _render_one('image:charts/br_particles_kinetics_snapshots.png', {}, RUNS_DB, STUDY_YAML))

# **br_particles_kinetics_particles_traces.png**

# br_particles_kinetics_particles_traces.png
_save_viz('br_particles_kinetics', 'br_particles_kinetics_particles_traces.png', _render_one('image:charts/br_particles_kinetics_particles_traces.png', {}, RUNS_DB, STUDY_YAML))

# **br_particles_kinetics_mass.png**

# br_particles_kinetics_mass.png
_save_viz('br_particles_kinetics', 'br_particles_kinetics_mass.png', _render_one('image:charts/br_particles_kinetics_mass.png', {}, RUNS_DB, STUDY_YAML))

# ### Acceptance criteria
#
# _Pre-registered checks (criteria/thresholds only — run the cells above to evaluate them)._
#
# | test | measures | passes if |
# | --- | --- | --- |
# | BR_PARTICLES_KINETICS-REPRODUCES-REPORT | kind=artifacts_present expected=['br_particles_kinetics_video.gif', 'br_particles_kinetics_mass.png', 'br_particles_kinetics_snapshots.png', 'br_particles_kinetics_particles_traces.png', 'br_particles_kinetics_viz.png', 'br_particles_kinetics_schema.json', 'br_particles_kinetics_state.json'] | op all_exist_and_match tolerance 0.02 |

# ## Study: `br_particles_dfba`
#
# **Purpose.** Runs the spatio_flux.composites.particles.br_particles_dfba composite (pair composition).

# ### Parameters
#
# | simulation | composite | steps | params |
# | --- | --- | --- | --- |
# | `baseline` | `spatio_flux.composites.particles.br_particles_dfba` | 0 | particle_model_id=ecoli core |

# ### Specification (process-bigraph) — load, inspect, edit
#
# Each composite is a process-bigraph *document*: named processes (`_type: process`) bound to an `address`, wired by `inputs`/`outputs` ports over shared stores. For every composite below the first cell loads the spec into a plain **editable Python dict** and prints its structure; the second cell is a **control panel** listing every configuration value and per-process `interval` so you can tweak any of them. Your edits are read when the composite is built and run, in the **Run** section.

# **Composite `spatio_flux.composites.particles.br_particles_dfba`** — `spec_spatio_flux_composites_particles_br_particles_dfba` (a plain, editable dict)

# _composite spec file for `spatio_flux.composites.particles.br_particles_dfba` not found under `spatio_flux/composites/` — skipped._

# ### Run
#
# _Set the runtime (`STEPS`) and step size (`INTERVAL`), then run. Each simulation builds the (edited) spec above and writes `runs.db`; the figures below read it. Set `RERUN = False` to skip re-simulating._

# === Study: br_particles_dfba ===
STUDY = 'br_particles_dfba'
STUDY_DIR = REPO / 'studies' / STUDY
STUDY_YAML = str(STUDY_DIR / "study.yaml")
RUNS_DB = str(STUDY_DIR / "runs.db")

print("No recorded runs for this study; nothing to reproduce.")

# ### Visualizations
#
# _Results are shown by the figures below, produced by the run above._

# **br_particles_dfba.svg**

# br_particles_dfba.svg
_save_viz('br_particles_dfba', 'br_particles_dfba.svg', _render_one('image:visualizations/br_particles_dfba.svg', {}, RUNS_DB, STUDY_YAML))

# **br_particles_dfba_snapshots.png**

# br_particles_dfba_snapshots.png
_save_viz('br_particles_dfba', 'br_particles_dfba_snapshots.png', _render_one('image:charts/br_particles_dfba_snapshots.png', {}, RUNS_DB, STUDY_YAML))

# **br_particles_dfba_particles_traces.png**

# br_particles_dfba_particles_traces.png
_save_viz('br_particles_dfba', 'br_particles_dfba_particles_traces.png', _render_one('image:charts/br_particles_dfba_particles_traces.png', {}, RUNS_DB, STUDY_YAML))

# **br_particles_dfba_mass.png**

# br_particles_dfba_mass.png
_save_viz('br_particles_dfba', 'br_particles_dfba_mass.png', _render_one('image:charts/br_particles_dfba_mass.png', {}, RUNS_DB, STUDY_YAML))

# ### Acceptance criteria
#
# _Pre-registered checks (criteria/thresholds only — run the cells above to evaluate them)._
#
# | test | measures | passes if |
# | --- | --- | --- |
# | BR_PARTICLES_DFBA-REPRODUCES-REPORT | kind=artifacts_present expected=['br_particles_dfba_video.gif', 'br_particles_dfba_mass.png', 'br_particles_dfba_snapshots.png', 'br_particles_dfba_particles_traces.png', 'br_particles_dfba_viz.png', 'br_particles_dfba_schema.json', 'br_particles_dfba_state.json'] | op all_exist_and_match tolerance 0.02 |

# ## Study: `comets_br_particles_kinetics`
#
# **Purpose.** Runs the spatio_flux.composites.comets.comets_br_particles_kinetics composite (triple composition).

# ### Parameters
#
# | simulation | composite | steps | params |
# | --- | --- | --- | --- |
# | `baseline` | `spatio_flux.composites.comets.comets_br_particles_kinetics` | 0 | — |

# ### Specification (process-bigraph) — load, inspect, edit
#
# Each composite is a process-bigraph *document*: named processes (`_type: process`) bound to an `address`, wired by `inputs`/`outputs` ports over shared stores. For every composite below the first cell loads the spec into a plain **editable Python dict** and prints its structure; the second cell is a **control panel** listing every configuration value and per-process `interval` so you can tweak any of them. Your edits are read when the composite is built and run, in the **Run** section.

# **Composite `spatio_flux.composites.comets.comets_br_particles_kinetics`** — `spec_spatio_flux_composites_comets_comets_br_particles_kinetics` (a plain, editable dict)

# _composite spec file for `spatio_flux.composites.comets.comets_br_particles_kinetics` not found under `spatio_flux/composites/` — skipped._

# ### Run
#
# _Set the runtime (`STEPS`) and step size (`INTERVAL`), then run. Each simulation builds the (edited) spec above and writes `runs.db`; the figures below read it. Set `RERUN = False` to skip re-simulating._

# === Study: comets_br_particles_kinetics ===
STUDY = 'comets_br_particles_kinetics'
STUDY_DIR = REPO / 'studies' / STUDY
STUDY_YAML = str(STUDY_DIR / "study.yaml")
RUNS_DB = str(STUDY_DIR / "runs.db")

print("No recorded runs for this study; nothing to reproduce.")

# ### Visualizations
#
# _Results are shown by the figures below, produced by the run above._

# **comets_br_particles_kinetics.svg**

# comets_br_particles_kinetics.svg
_save_viz('comets_br_particles_kinetics', 'comets_br_particles_kinetics.svg', _render_one('image:visualizations/comets_br_particles_kinetics.svg', {}, RUNS_DB, STUDY_YAML))

# **comets_br_particles_kinetics_timeseries.png**

# comets_br_particles_kinetics_timeseries.png
_save_viz('comets_br_particles_kinetics', 'comets_br_particles_kinetics_timeseries.png', _render_one('image:charts/comets_br_particles_kinetics_timeseries.png', {}, RUNS_DB, STUDY_YAML))

# **comets_br_particles_kinetics_snapshots.png**

# comets_br_particles_kinetics_snapshots.png
_save_viz('comets_br_particles_kinetics', 'comets_br_particles_kinetics_snapshots.png', _render_one('image:charts/comets_br_particles_kinetics_snapshots.png', {}, RUNS_DB, STUDY_YAML))

# **comets_br_particles_kinetics_mass.png**

# comets_br_particles_kinetics_mass.png
_save_viz('comets_br_particles_kinetics', 'comets_br_particles_kinetics_mass.png', _render_one('image:charts/comets_br_particles_kinetics_mass.png', {}, RUNS_DB, STUDY_YAML))

# ### Acceptance criteria
#
# _Pre-registered checks (criteria/thresholds only — run the cells above to evaluate them)._
#
# | test | measures | passes if |
# | --- | --- | --- |
# | COMETS_BR_PARTICLES_KINETICS-REPRODUCES-REPORT | kind=artifacts_present expected=['comets_br_particles_kinetics_timeseries.png', 'comets_br_particles_kinetics_mass.png', 'comets_br_particles_kinetics_snapshots.png', 'comets_br_particles_kinetics_video.gif', 'comets_br_particles_kinetics_viz.png', 'comets_br_particles_kinetics_schema.json', 'comets_br_particles_kinetics_state.json'] | op all_exist_and_match tolerance 0.02 |

# ## Study: `comets_br_particles_dfba`
#
# **Purpose.** Runs the spatio_flux.composites.comets.comets_br_particles_dfba composite (triple composition).

# ### Parameters
#
# | simulation | composite | steps | params |
# | --- | --- | --- | --- |
# | `baseline` | `spatio_flux.composites.comets.comets_br_particles_dfba` | 0 | — |

# ### Specification (process-bigraph) — load, inspect, edit
#
# Each composite is a process-bigraph *document*: named processes (`_type: process`) bound to an `address`, wired by `inputs`/`outputs` ports over shared stores. For every composite below the first cell loads the spec into a plain **editable Python dict** and prints its structure; the second cell is a **control panel** listing every configuration value and per-process `interval` so you can tweak any of them. Your edits are read when the composite is built and run, in the **Run** section.

# **Composite `spatio_flux.composites.comets.comets_br_particles_dfba`** — `spec_spatio_flux_composites_comets_comets_br_particles_dfba` (a plain, editable dict)

# _composite spec file for `spatio_flux.composites.comets.comets_br_particles_dfba` not found under `spatio_flux/composites/` — skipped._

# ### Run
#
# _Set the runtime (`STEPS`) and step size (`INTERVAL`), then run. Each simulation builds the (edited) spec above and writes `runs.db`; the figures below read it. Set `RERUN = False` to skip re-simulating._

# === Study: comets_br_particles_dfba ===
STUDY = 'comets_br_particles_dfba'
STUDY_DIR = REPO / 'studies' / STUDY
STUDY_YAML = str(STUDY_DIR / "study.yaml")
RUNS_DB = str(STUDY_DIR / "runs.db")

print("No recorded runs for this study; nothing to reproduce.")

# ### Visualizations
#
# _Results are shown by the figures below, produced by the run above._

# **comets_br_particles_dfba.svg**

# comets_br_particles_dfba.svg
_save_viz('comets_br_particles_dfba', 'comets_br_particles_dfba.svg', _render_one('image:visualizations/comets_br_particles_dfba.svg', {}, RUNS_DB, STUDY_YAML))

# **comets_br_particles_dfba_timeseries.png**

# comets_br_particles_dfba_timeseries.png
_save_viz('comets_br_particles_dfba', 'comets_br_particles_dfba_timeseries.png', _render_one('image:charts/comets_br_particles_dfba_timeseries.png', {}, RUNS_DB, STUDY_YAML))

# **comets_br_particles_dfba_snapshots.png**

# comets_br_particles_dfba_snapshots.png
_save_viz('comets_br_particles_dfba', 'comets_br_particles_dfba_snapshots.png', _render_one('image:charts/comets_br_particles_dfba_snapshots.png', {}, RUNS_DB, STUDY_YAML))

# **comets_br_particles_dfba_mass.png**

# comets_br_particles_dfba_mass.png
_save_viz('comets_br_particles_dfba', 'comets_br_particles_dfba_mass.png', _render_one('image:charts/comets_br_particles_dfba_mass.png', {}, RUNS_DB, STUDY_YAML))

# ### Acceptance criteria
#
# _Pre-registered checks (criteria/thresholds only — run the cells above to evaluate them)._
#
# | test | measures | passes if |
# | --- | --- | --- |
# | COMETS_BR_PARTICLES_DFBA-REPRODUCES-REPORT | kind=artifacts_present expected=['comets_br_particles_dfba_timeseries.png', 'comets_br_particles_dfba_mass.png', 'comets_br_particles_dfba_snapshots.png', 'comets_br_particles_dfba_video.gif', 'comets_br_particles_dfba_viz.png', 'comets_br_particles_dfba_schema.json', 'comets_br_particles_dfba_state.json'] | op all_exist_and_match tolerance 0.02 |

# ## Study: `comets_nt_particles_dfba`
#
# **Purpose.** Runs the spatio_flux.composites.comets.comets_nt_particles_dfba composite (triple composition).

# ### Parameters
#
# | simulation | composite | steps | params |
# | --- | --- | --- | --- |
# | `baseline` | `spatio_flux.composites.comets.comets_nt_particles_dfba` | 0 | — |

# ### Specification (process-bigraph) — load, inspect, edit
#
# Each composite is a process-bigraph *document*: named processes (`_type: process`) bound to an `address`, wired by `inputs`/`outputs` ports over shared stores. For every composite below the first cell loads the spec into a plain **editable Python dict** and prints its structure; the second cell is a **control panel** listing every configuration value and per-process `interval` so you can tweak any of them. Your edits are read when the composite is built and run, in the **Run** section.

# **Composite `spatio_flux.composites.comets.comets_nt_particles_dfba`** — `spec_spatio_flux_composites_comets_comets_nt_particles_dfba` (a plain, editable dict)

# _composite spec file for `spatio_flux.composites.comets.comets_nt_particles_dfba` not found under `spatio_flux/composites/` — skipped._

# ### Run
#
# _Set the runtime (`STEPS`) and step size (`INTERVAL`), then run. Each simulation builds the (edited) spec above and writes `runs.db`; the figures below read it. Set `RERUN = False` to skip re-simulating._

# === Study: comets_nt_particles_dfba ===
STUDY = 'comets_nt_particles_dfba'
STUDY_DIR = REPO / 'studies' / STUDY
STUDY_YAML = str(STUDY_DIR / "study.yaml")
RUNS_DB = str(STUDY_DIR / "runs.db")

print("No recorded runs for this study; nothing to reproduce.")

# ### Visualizations
#
# _Results are shown by the figures below, produced by the run above._

# **comets_nt_particles_dfba.svg**

# comets_nt_particles_dfba.svg
_save_viz('comets_nt_particles_dfba', 'comets_nt_particles_dfba.svg', _render_one('image:visualizations/comets_nt_particles_dfba.svg', {}, RUNS_DB, STUDY_YAML))

# **comets_nt_particles_dfba_timeseries.png**

# comets_nt_particles_dfba_timeseries.png
_save_viz('comets_nt_particles_dfba', 'comets_nt_particles_dfba_timeseries.png', _render_one('image:charts/comets_nt_particles_dfba_timeseries.png', {}, RUNS_DB, STUDY_YAML))

# **comets_nt_particles_dfba_snapshots.png**

# comets_nt_particles_dfba_snapshots.png
_save_viz('comets_nt_particles_dfba', 'comets_nt_particles_dfba_snapshots.png', _render_one('image:charts/comets_nt_particles_dfba_snapshots.png', {}, RUNS_DB, STUDY_YAML))

# **comets_nt_particles_dfba_mass.png**

# comets_nt_particles_dfba_mass.png
_save_viz('comets_nt_particles_dfba', 'comets_nt_particles_dfba_mass.png', _render_one('image:charts/comets_nt_particles_dfba_mass.png', {}, RUNS_DB, STUDY_YAML))

# **comets_nt_particles_dfba_mass_submasses.png**

# comets_nt_particles_dfba_mass_submasses.png
_save_viz('comets_nt_particles_dfba', 'comets_nt_particles_dfba_mass_submasses.png', _render_one('image:charts/comets_nt_particles_dfba_mass_submasses.png', {}, RUNS_DB, STUDY_YAML))

# ### Acceptance criteria
#
# _Pre-registered checks (criteria/thresholds only — run the cells above to evaluate them)._
#
# | test | measures | passes if |
# | --- | --- | --- |
# | COMETS_NT_PARTICLES_DFBA-REPRODUCES-REPORT | kind=artifacts_present expected=['comets_nt_particles_dfba_timeseries.png', 'comets_nt_particles_dfba_mass.png', 'comets_nt_particles_dfba_mass_submasses.png', 'comets_nt_particles_dfba_video.gif', 'comets_nt_particles_dfba_snapshots.png', 'comets_nt_particles_dfba_viz.png', 'comets_nt_particles_dfba_schema.json', 'comets_nt_particles_dfba_state.json'] | op all_exist_and_match tolerance 0.02 |

# ## Study: `spatioflux_reference_demo`
#
# **Purpose.** Runs the spatio_flux.composites.reference.spatioflux_reference_demo composite (reference demo).

# ### Parameters
#
# | simulation | composite | steps | params |
# | --- | --- | --- | --- |
# | `baseline` | `spatio_flux.composites.reference.spatioflux_reference_demo` | 0 | n_bins=[10, 10] |

# ### Specification (process-bigraph) — load, inspect, edit
#
# Each composite is a process-bigraph *document*: named processes (`_type: process`) bound to an `address`, wired by `inputs`/`outputs` ports over shared stores. For every composite below the first cell loads the spec into a plain **editable Python dict** and prints its structure; the second cell is a **control panel** listing every configuration value and per-process `interval` so you can tweak any of them. Your edits are read when the composite is built and run, in the **Run** section.

# **Composite `spatio_flux.composites.reference.spatioflux_reference_demo`** — `spec_spatio_flux_composites_reference_spatioflux_reference_demo` (a plain, editable dict)

# _composite spec file for `spatio_flux.composites.reference.spatioflux_reference_demo` not found under `spatio_flux/composites/` — skipped._

# ### Run
#
# _Set the runtime (`STEPS`) and step size (`INTERVAL`), then run. Each simulation builds the (edited) spec above and writes `runs.db`; the figures below read it. Set `RERUN = False` to skip re-simulating._

# === Study: spatioflux_reference_demo ===
STUDY = 'spatioflux_reference_demo'
STUDY_DIR = REPO / 'studies' / STUDY
STUDY_YAML = str(STUDY_DIR / "study.yaml")
RUNS_DB = str(STUDY_DIR / "runs.db")

print("No recorded runs for this study; nothing to reproduce.")

# ### Visualizations
#
# _Results are shown by the figures below, produced by the run above._

# **spatioflux_reference_demo.svg**

# spatioflux_reference_demo.svg
_save_viz('spatioflux_reference_demo', 'spatioflux_reference_demo.svg', _render_one('image:visualizations/spatioflux_reference_demo.svg', {}, RUNS_DB, STUDY_YAML))

# **spatioflux_reference_demo_timeseries.png**

# spatioflux_reference_demo_timeseries.png
_save_viz('spatioflux_reference_demo', 'spatioflux_reference_demo_timeseries.png', _render_one('image:charts/spatioflux_reference_demo_timeseries.png', {}, RUNS_DB, STUDY_YAML))

# **spatioflux_reference_demo_snapshots.png**

# spatioflux_reference_demo_snapshots.png
_save_viz('spatioflux_reference_demo', 'spatioflux_reference_demo_snapshots.png', _render_one('image:charts/spatioflux_reference_demo_snapshots.png', {}, RUNS_DB, STUDY_YAML))

# **spatioflux_reference_demo_mass.png**

# spatioflux_reference_demo_mass.png
_save_viz('spatioflux_reference_demo', 'spatioflux_reference_demo_mass.png', _render_one('image:charts/spatioflux_reference_demo_mass.png', {}, RUNS_DB, STUDY_YAML))

# **spatioflux_reference_demo_mass_submasses.png**

# spatioflux_reference_demo_mass_submasses.png
_save_viz('spatioflux_reference_demo', 'spatioflux_reference_demo_mass_submasses.png', _render_one('image:charts/spatioflux_reference_demo_mass_submasses.png', {}, RUNS_DB, STUDY_YAML))

# ### Acceptance criteria
#
# _Pre-registered checks (criteria/thresholds only — run the cells above to evaluate them)._
#
# | test | measures | passes if |
# | --- | --- | --- |
# | SPATIOFLUX_REFERENCE_DEMO-REPRODUCES-REPORT | kind=artifacts_present expected=['spatioflux_reference_demo_timeseries.png', 'spatioflux_reference_demo_mass.png', 'spatioflux_reference_demo_mass_submasses.png', 'spatioflux_reference_demo_video.gif', 'spatioflux_reference_demo_snapshots.png', 'spatioflux_reference_demo_viz.png', 'spatioflux_reference_demo_schema.json', 'spatioflux_reference_demo_state.json'] | op all_exist_and_match tolerance 0.02 |

# ## Study: `reference_demo_x2y2`
#
# **Purpose.** Runs the spatio_flux.composites.reference.reference_demo_x2y2 composite (reference demo).

# ### Parameters
#
# | simulation | composite | steps | params |
# | --- | --- | --- | --- |
# | `baseline` | `spatio_flux.composites.reference.reference_demo_x2y2` | 0 | n_bins=[20, 20] |

# ### Specification (process-bigraph) — load, inspect, edit
#
# Each composite is a process-bigraph *document*: named processes (`_type: process`) bound to an `address`, wired by `inputs`/`outputs` ports over shared stores. For every composite below the first cell loads the spec into a plain **editable Python dict** and prints its structure; the second cell is a **control panel** listing every configuration value and per-process `interval` so you can tweak any of them. Your edits are read when the composite is built and run, in the **Run** section.

# **Composite `spatio_flux.composites.reference.reference_demo_x2y2`** — `spec_spatio_flux_composites_reference_reference_demo_x2y2` (a plain, editable dict)

# _composite spec file for `spatio_flux.composites.reference.reference_demo_x2y2` not found under `spatio_flux/composites/` — skipped._

# ### Run
#
# _Set the runtime (`STEPS`) and step size (`INTERVAL`), then run. Each simulation builds the (edited) spec above and writes `runs.db`; the figures below read it. Set `RERUN = False` to skip re-simulating._

# === Study: reference_demo_x2y2 ===
STUDY = 'reference_demo_x2y2'
STUDY_DIR = REPO / 'studies' / STUDY
STUDY_YAML = str(STUDY_DIR / "study.yaml")
RUNS_DB = str(STUDY_DIR / "runs.db")

print("No recorded runs for this study; nothing to reproduce.")

# ### Visualizations
#
# _Results are shown by the figures below, produced by the run above._

# **reference_demo_x2y2.svg**

# reference_demo_x2y2.svg
_save_viz('reference_demo_x2y2', 'reference_demo_x2y2.svg', _render_one('image:visualizations/reference_demo_x2y2.svg', {}, RUNS_DB, STUDY_YAML))

# **reference_demo_x2y2_timeseries.png**

# reference_demo_x2y2_timeseries.png
_save_viz('reference_demo_x2y2', 'reference_demo_x2y2_timeseries.png', _render_one('image:charts/reference_demo_x2y2_timeseries.png', {}, RUNS_DB, STUDY_YAML))

# **reference_demo_x2y2_snapshots.png**

# reference_demo_x2y2_snapshots.png
_save_viz('reference_demo_x2y2', 'reference_demo_x2y2_snapshots.png', _render_one('image:charts/reference_demo_x2y2_snapshots.png', {}, RUNS_DB, STUDY_YAML))

# **reference_demo_x2y2_mass.png**

# reference_demo_x2y2_mass.png
_save_viz('reference_demo_x2y2', 'reference_demo_x2y2_mass.png', _render_one('image:charts/reference_demo_x2y2_mass.png', {}, RUNS_DB, STUDY_YAML))

# **reference_demo_x2y2_mass_submasses.png**

# reference_demo_x2y2_mass_submasses.png
_save_viz('reference_demo_x2y2', 'reference_demo_x2y2_mass_submasses.png', _render_one('image:charts/reference_demo_x2y2_mass_submasses.png', {}, RUNS_DB, STUDY_YAML))

# ### Acceptance criteria
#
# _Pre-registered checks (criteria/thresholds only — run the cells above to evaluate them)._
#
# | test | measures | passes if |
# | --- | --- | --- |
# | REFERENCE_DEMO_X2Y2-REPRODUCES-REPORT | kind=artifacts_present expected=['reference_demo_x2y2_timeseries.png', 'reference_demo_x2y2_mass.png', 'reference_demo_x2y2_mass_submasses.png', 'reference_demo_x2y2_video.gif', 'reference_demo_x2y2_snapshots.png', 'reference_demo_x2y2_viz.png', 'reference_demo_x2y2_schema.json', 'reference_demo_x2y2_state.json'] | op all_exist_and_match tolerance 0.02 |
