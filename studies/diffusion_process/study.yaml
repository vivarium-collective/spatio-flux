schema_version: 3
name: diffusion_process
created: '2026-07-26'
status: complete
phase: Evaluate
design_status: complete
implementation_status: complete
simulation_status: ran
evaluation_status: evaluated
gate_status: passed
baseline:
- name: baseline
  composite: spatio_flux.composites.spatial.diffusion_process
  params: {}
purpose:
  question: 'Field transport primitive: finite-volume diffusion/advection on a 2D
    lattice. Use to validate boundary conditions, stability, and transport timescales.'
  mechanism: Runs the spatio_flux.composites.spatial.diffusion_process composite (standalone
    process).
  expected_outcome: Reproduces the current test-suite report artifacts for this scenario.
pipeline_gate:
  prerequisites: []
  enables: []
  proceed_condition: Composition ordering only; this study runs independently.
limitations:
- Faithful reproduction of an existing scenario; no new biology.
- Graph edges document composition, they do not gate execution.
canonical_runs:
- name: reproduce
  script: spatio_flux/runners/run_study.py
  args:
  - diffusion_process
  - '60'
  label: reproduce diffusion_process figures
  default: true
visualizations:
- name: diffusion_process.svg
  address: image:visualizations/diffusion_process.svg
  chart: image
behavior_tests:
- name: DIFFUSION_PROCESS-REPRODUCES-REPORT
  classification: regression
  description: All test-suite artifacts for this scenario are reproduced and match
    the out0 reference within tolerance.
  measure:
    kind: artifacts_present
    expected:
    - diffusion_process_video.gif
    - diffusion_process_viz.png
    - diffusion_process_schema.json
    - diffusion_process_state.json
  pass_if:
    op: all_exist_and_match
    tolerance: 0.02
  requires_simulation: reproduce
runs:
- name: diffusion_process-reproduce
  run_id: diffusion_process-reproduce
  kind: simulation
  status: completed
  canonical: true
  composite: spatio_flux.composites.spatial.diffusion_process
  emitter:
    kind: xarray
    store: studies/diffusion_process/runs.diffusion_process-reproduce.zarr
  store_path: studies/diffusion_process/runs.diffusion_process-reproduce.zarr
  timestamp: '2026-07-26T12:00:00Z'
  result: PASS
  outcomes:
    DIFFUSION_PROCESS-REPRODUCES-REPORT:
      result: PASS
      detail: All 4 report artifacts reproduced via the post-run analysis flush; deterministic
        scenarios match the out0 reference within tolerance.
findings:
- id: F1
  kind: computational
  status: passed
  statement: spatio_flux reproduces the diffusion_process test-suite report artifacts
    via the post-run analysis flush.
  evidence:
    from_test: DIFFUSION_PROCESS-REPRODUCES-REPORT
  provenance:
    run_ids:
    - diffusion_process-reproduce
conclusion_logic:
  if_primary_tests_pass:
    implementation_status: The diffusion_process scenario is faithfully reproduced
      by the investigation's post-run analysis flush.
    biological_validation: Validates that the composition reproduces the reference
      test-suite artifacts; it does not add new biological claims beyond the original
      scenario.
    pipeline_unblocks: []
  if_primary_tests_fail:
    diagnose:
    - Compare the produced charts/<slug>_* artifacts against out0/.
    - Check the composite build and the FLUSH_SPEC config for this slug.
    block_downstream: Downstream composition studies that reuse this scenario should
      be re-examined.
