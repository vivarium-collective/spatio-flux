schema_version: 3
name: spatial_many_dfba
created: '2026-07-26'
status: complete
phase: Evaluate
design_status: complete
implementation_status: complete
simulation_status: ran
evaluation_status: evaluated
gate_status: passed
baseline:
- name: baseline
  composite: spatio_flux.composites.spatial.spatial_many_dfba
  params:
    model_id: ecoli core
purpose:
  question: 'Spatial microenvironment (sitewise dFBA): a lattice where each site runs
    its own dFBA instance. Useful for validating lattice indexing + per-site state
    isolation.'
  mechanism: Runs the spatio_flux.composites.spatial.spatial_many_dfba composite (pair
    composition).
  expected_outcome: Reproduces the current test-suite report artifacts for this scenario.
pipeline_gate:
  prerequisites:
  - ecoli_core_dfba
  enables: []
  proceed_condition: Composition ordering only; this study runs independently.
limitations:
- Faithful reproduction of an existing scenario; no new biology.
- Graph edges document composition, they do not gate execution.
canonical_runs:
- name: reproduce
  script: spatio_flux/runners/run_study.py
  args:
  - spatial_many_dfba
  - '60'
  label: reproduce spatial_many_dfba figures
  default: true
visualizations:
- name: spatial_many_dfba.svg
  address: image:visualizations/spatial_many_dfba.svg
  chart: image
- name: spatial_many_dfba_timeseries.png
  address: image:charts/spatial_many_dfba_timeseries.png
  chart: image
behavior_tests:
- name: SPATIAL_MANY_DFBA-REPRODUCES-REPORT
  classification: regression
  description: All 5 expected artifacts are produced in charts/, and the composite's
    structural schema (spatial_many_dfba_schema.json) matches the committed reference
    in studies/<slug>/reference/. State/figures embed RNG or non-deterministic renders,
    so they are existence-checked.
  measure:
    kind: artifacts_present
    expected:
    - spatial_many_dfba_timeseries.png
    - spatial_many_dfba_video.gif
    - spatial_many_dfba_viz.png
    - spatial_many_dfba_schema.json
    - spatial_many_dfba_state.json
  pass_if:
    op: all_exist_and_match
    tolerance: 0.0
  requires_simulation: reproduce
runs:
- name: spatial_many_dfba-reproduce
  run_id: spatial_many_dfba-reproduce
  kind: simulation
  status: completed
  canonical: true
  composite: spatio_flux.composites.spatial.spatial_many_dfba
  emitter:
    kind: xarray
    store: studies/spatial_many_dfba/runs.spatial_many_dfba-reproduce.zarr
  store_path: studies/spatial_many_dfba/runs.spatial_many_dfba-reproduce.zarr
  timestamp: '2026-09-20T15:24:04Z'
  result: PASS
  outcomes:
    SPATIAL_MANY_DFBA-REPRODUCES-REPORT:
      result: PASS
      evaluated_by: code
      detail: 5/5 artifacts present; spatial_many_dfba_schema.json matches reference
        (structural, tol=0.0)
findings:
- id: F1
  kind: computational
  status: passed
  statement: spatio_flux reproduces the spatial_many_dfba test-suite artifacts and
    the composite's structural schema matches its committed reference.
  evidence:
    from_test: SPATIAL_MANY_DFBA-REPRODUCES-REPORT
  provenance:
    run_ids:
    - spatial_many_dfba-reproduce
conclusion_logic:
  if_primary_tests_pass:
    implementation_status: The spatial_many_dfba scenario is faithfully reproduced
      by the investigation's post-run analysis flush.
    biological_validation: Validates that the composition reproduces the reference
      test-suite artifacts; it does not add new biological claims beyond the original
      scenario.
    pipeline_unblocks: []
  if_primary_tests_fail:
    diagnose:
    - Compare charts/<slug>_schema.json against studies/<slug>/reference/.
    - Check the composite build and the FLUSH_SPEC config for this slug.
    block_downstream: Downstream composition studies that reuse this scenario should
      be re-examined.
